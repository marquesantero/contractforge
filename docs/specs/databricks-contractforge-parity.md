# Databricks ContractForge Parity

## Purpose

The Databricks adapter should not lose mature Databricks adapter capabilities from the Databricks adapter baseline.

The target is not a byte-for-byte copy. The target is equivalent behavior and evidence:

- same ContractForge contract semantics
- same Databricks execution capabilities where they are still Databricks-owned
- same control/evidence discipline
- no Databricks/Spark dependency leaking into `contractforge_core`

## Parity Status

| Status | Meaning |
| --- | --- |
| `PORTED` | Equivalent behavior exists in `contractforge_databricks`. |
| `PORTED_IN_CORE` | Equivalent behavior exists in the platform-neutral ContractForge core/CLI and should not move into the Databricks adapter. |
| `PORTED_AS_REVIEW_ARTIFACT` | The adapter renders auditable artifacts, but execution remains runtime/deployment-owned. |
| `PORT_IN_PROGRESS` | Partially ported; known work remains. |
| `INTENTIONALLY_ADAPTER_OWNED` | Capability exists only in Databricks adapter and must not move to core. |
| `NOT_PORTED_YET` | mature Databricks capability still needs adapter implementation. |

## Capability Matrix

| Databricks adapter baseline capability | Databricks adapter status | Adapter modules | Notes |
| --- | --- | --- | --- |
| Databricks runtime capability classification | `PORTED` | `capabilities/`, `runtime/` | Passive runtime evidence, mature classic/serverless classification from Spark conf, Databricks job metadata and non-secret environment evidence collected by runtime detection, UC target shape, Lakeflow/Auto Loader/Connections/Volumes/Liquid eligibility and Unity Catalog capability issue reporting. |
| Databricks-specific contract fields | `PORTED` | `contract_extensions.py`, `write_modes/strategy.py`, `rendering/markdown.py` | Adapter-owned fields such as `cluster_columns`, `delta_properties`, `write_engine`, `zorder_columns`, encoding, cache, explain and OpenLineage options are moved into `extensions.databricks`; the core carries them opaquely and the adapter renders review evidence. Explicit Databricks `write_engine` requests participate in adapter strategy/fallback selection. |
| Contract planning against Databricks capabilities | `PORTED` | `adapter.py`, `write_modes/strategy.py` | Uses core planner and adapter capability mapping. |
| Delta append and overwrite | `PORTED` | `execution/delta_basic.py` | SQL runner based. |
| Delta table creation/properties setup | `PORTED` | `execution/tables.py` | SQL helpers create Delta tables from prepared schema, apply partitioning when clustering is not requested, render `CLUSTER BY`, and apply adapter-owned Delta table properties from `extensions.databricks`. |
| current-state Delta MERGE | `PORTED` | `execution/sql_merge.py`, `runtime/partitioning.py` | SQL runner based, including Databricks `delta_by_partition` target predicate scoping from affected prepared-source partition values. |
| hash-diff upsert | `PORTED` | core `semantic`, `preparation`; adapter `preparation/hashing.py`, `preparation/pyspark_staging.py`, `execution/hash_diff.py`, `execution/hash_diff_latest.py` | ContractForge algorithm preserved; runtime prepares row-hash staging, honors portable `hash_exclude_columns`, and final write is SQL with prepared-source key/hash validation before execution. The Databricks executor compares against the latest target version per hash key using, in order, matching `transform.deduplicate.order_by`, target `ingestion_sequence`, or target `ingestion_ts_utc`/`__run_id`. It rejects ambiguous legacy histories with null `ingestion_ts_utc`, and rejects duplicate target versions when no deterministic ordering is available and runtime querying is enabled. |
| Historical | `PORTED` | core `semantic`, `preparation`; adapter `preparation/pyspark_staging.py`, `execution/scd2.py`, `execution/scd2_deletes.py`, `execution/scd2_late.py` | ContractForge-compatible row-hash/control-column preparation, including portable `hash_exclude_columns`, `scd2_effective_from_column`, `scd2_late_arriving_policy` and `scd2_apply_as_deletes`, plus Databricks SQL target-aware staging. The executor creates changed insert/update staging rows before Delta MERGE so unchanged current rows are not duplicated and old current versions are expired before inserting new versions. Late-arriving `ignore` filters stale sequence rows; `reject` fails the staging SQL when stale rows are detected. Delete expressions expire current rows with `changed_columns='DELETE'` and are excluded from the upsert stage. |
| Snapshot soft delete | `PORTED` | `preparation/pyspark_staging.py`, `execution/snapshot.py` | Runtime prepares snapshot control columns and row hash; Delta `NOT MATCHED BY SOURCE`-style behavior keeps review caveats and prepared-source validation. |
| Merge source safety checks | `PORTED` | `runtime/merge_validation.py`, `runtime/write_flow.py` | SQL preflight over prepared views rejects fully-null merge keys and duplicate source key groups before Databricks MERGE writes. If an equivalent `quality_rules.unique_key` already passed, duplicate probing follows the deduplicated validation skip behavior. |
| Native write-engine parity catalog | `PORTED` | core `execution/write_modes.py`; adapter `parity/`, `write_modes/` | Tracks native/non-equivalence scenarios. Core owns canonical custom write mode naming and stable strategy labels; Databricks owns adapter-specific handler registration and runtime dispatch. |
| Lakeflow AUTO CDC compatibility | `PORTED` | `lakeflow/` | Change-feed and snapshot renderer plus compatibility artifact, including review markers for projection, column mapping, filter, watermark, shape, transform and quality semantics that must be materialized before AUTO CDC. The adapter reads `merge_keys` and `scd2_sequence_by` from the core contract by default, while still allowing Databricks-specific overrides for reviewed Lakeflow artifacts. |
| Auto Loader / incremental files | `PORTED` | `sources/autoloader.py`, `sources/interpret.py` | Portable `incremental_files` maps to Databricks `cloudFiles`. |
| File/object/catalog/JDBC/HTTP sources | `PORTED` | `sources/`, `runtime/sources.py`, `runtime/file_selection.py`, `runtime/source_schema.py`, `runtime/storage_auth.py` | Rendered Databricks read artifacts and runtime source resolution. File/object-storage runtime preserves `source.read.file_regex` selection semantics through Hadoop FileSystem listing in classic runtimes, or through an explicit `source.read.files` list when Spark listing is unavailable. File, HTTP file and bounded REST runtime apply declared source schemas from canonical `source.read.schema`; top-level `source.schema` is not accepted in the new core product. Object-storage runtime also ports adapter-owned S3 credential/Hadoop option configuration, Azure Blob SAS configuration/path normalization and clear serverless/Spark Connect guidance when direct Hadoop credential configuration is blocked, while keeping secret resolution and Spark config out of the core. |
| REST API connector boundary | `PORTED` | `sources/rest_api.py`, `runtime/rest_api.py`, `runtime/json_materialization.py` | Bundle rendering remains review-first for arbitrary/proprietary APIs, while bounded JSON REST execution is available as adapter runtime for explicit use. Runtime materialization preserves the JSON Lines/Spark-reader fallback for complex records and declared schemas instead of relying only on direct `createDataFrame`. |
| Native passthrough | `PORTED_AS_REVIEW_ARTIFACT` | `sources/native_passthrough.py` | Guides Lakeflow Connect/Connections/native platform use. |
| Bounded Kafka/Event Hubs replay | `PORTED` | `sources/bounded_streams.py` | Batch replay only; continuous streaming remains review-required. |
| Delta Sharing consumer | `PORTED` | `sources/delta_share.py` | Databricks `deltaSharing` artifact. |
| Schema policy planning and runtime preflight diff | `PORTED` | core `schema/`; adapter `schema/`, `runtime/schema.py`, `execution/tables.py` | Strict/additive/permissive diff, case-insensitive numeric widening checks, Delta DDL helpers, cluster/partition column validation, runtime schema setup/sync from prepared schema and `ctrl_ingestion_schema_changes` evidence. Schema-change rows preserve timestamps, framework/control schema version, raw payload JSON and prepared-source type for additive columns when available. Applied type-widening DDL is marked with `applied=true` in the returned schema-change payload and evidence rows. Target-only ContractForge control columns are ignored during removed-column detection, and legacy physical metadata columns (`ingestion_date`, `source_system`, `ingestion_sequence`) are tolerated only when already present on existing targets. |
| Quality checks and quarantine evidence | `PORTED` | core `contracts`, `normalization`, `quality`, `runtime`; adapter `quality/`, `runtime/options.py`, `runtime/finalization.py` | SQL checks, runtime evaluation for portable quality intents, result normalization and control-table evidence inserts. Contract-level `on_quality_fail` is accepted by the core and mapped by the Databricks runtime to its effective quality action. Runtime finalization persists `ctrl_ingestion_quality` rows with severity, failed count, observed payload, message and details even when quality failure stops the target write. Prepared runtime inputs can carry platform-neutral quarantine references; the Databricks adapter writes them to `ctrl_ingestion_quarantine` without requiring the core to know Spark or Delta payload storage. The guardrail for large inline `accepted_values` lists is preserved so users must use a reference table or custom evaluator instead of generating oversized Spark/SQL predicates. Abort-only rule classification is core semantics; custom evaluator registration remains adapter-owned because quarantine evaluators may return Spark conditions. |
| Custom quality rule registry | `PORTED` | core `contracts/quality.py`, `contracts/normalize.py`; adapter `quality/registry.py`, `quality/evaluation.py` | Adapter-owned runtime extension point for custom DataFrame evaluators, with register/get/list/unregister lifecycle helpers. The core validates `quality_rules.custom` and preserves evaluator-specific parameters as opaque `extensions.quality.custom`; Databricks executes them at runtime and can apply custom quarantine conditions without making those rules portable core semantics. |
| Shape intent | `PORTED` | `shapes/`, `preparation/shape.py`, `preparation/shape_validation.py` | Review SQL plus PySpark runtime execution for portable shape operations. |
| Transform intent | `PORTED` | core `contracts.normalize`; adapter `transforms/`, `preparation/pyspark.py` | Review SQL plus runtime PySpark execution for cast, standardize, derive, composite key construction and deduplicate. Column mapping validates missing sources, duplicate targets, collisions and reserved technical target columns before PySpark execution. Deduplication is canonical in `transform.deduplicate`; derived key construction is canonical in `transform.composite_keys`, with no legacy top-level aliases in the new core product. |
| Stable row hashing | `PORTED` | core `preparation`; adapter `preparation/hashing.py`, `preparation/pyspark_staging.py` | The core defines ContractForge hash delimiter/sentinel semantics using Unit Separator and NUL to avoid delimiter/null collisions. The Databricks adapter renders the same semantics in SQL/PySpark staging for hash-diff, historical and snapshot modes. |
| Watermarks | `PORTED` | core `watermark.py`; adapter `watermark/`, `state/`, `runtime/rest_api.py`, `runtime/watermark.py`, `runtime/sources.py`, `preparation/pyspark.py` | Typed watermark encoding/decoding, single-field extraction for connector runtime parameters, JDBC predicate pushdown, REST query/header/body injection, previous-state lookup, prepared-view filtering after mapping/transforms and before deduplicate, predicate/candidate SQL, runtime candidate collection from prepared views, `watermark_previous`/`watermark_current` run evidence and state upsert. |
| Partition predicates and scoped overwrite | `PORTED` | `partitioning/`, `runtime/partitioning.py`, `execution/replace_partitions.py` | Databricks predicate rendering, runtime current-state target partition scoping for `merge_strategy=delta_by_partition`, and complete-partition selective overwrite for `merge_strategy=replace_partitions` using Databricks SQL `REPLACE WHERE`. |
| Maintenance SQL and post-write optimize | `PORTED` | `maintenance/`, `runtime/maintenance.py` | Delta properties, optimize, ZORDER, vacuum, analyze, runtime `optimize_after_write` execution and control-table retention planning, including adapter-owned `ctrl_ingestion_cost` evidence retention. |
| Control/evidence table DDL | `PORTED` | `evidence/schemas.py`, `evidence/ddl.py`, `state/ddl.py`, `diagnostics/` | New tables use mature ContractForge control columns. The shared evidence catalog includes `ctrl_ingestion_explain` so tooling that enumerates evidence tables does not miss diagnostics, while the diagnostics renderer still exposes the focused explain-table DDL artifact. |
| Additive control-table migrations | `PORTED` | `state/migrations.py` | Existing installations can add missing columns. |
| Full run ledger logging | `PORTED` | `evidence/run_log.py`, `evidence/writer.py`, `runtime/run_payload.py`, `runtime/write_flow.py`, `runtime/write_strategy.py` | Complete `ctrl_ingestion_runs` payload renderer with redaction. Runtime payloads preserve source identity, write-engine selection evidence, hierarchy metadata, write start/finish timestamps and stage duration breakdowns for schema, preflight, write, maintenance and governance. Runtime finalization also records current framework/control schema metadata in `ctrl_ingestion_metadata`. |
| Error/schema/stream logging | `PORTED` | `evidence/ops_log.py`, `runtime/errors.py`, `runtime/orchestrator.py` | Full operational control-table SQL renderers. Runtime failures populate `ctrl_ingestion_errors` with redacted short message, full redacted stack trace, `error_type`, `error_class`, `occurred_at_utc` and runtime metadata when available. |
| Streaming metrics and lifecycle payloads | `PORTED` | `runtime/streaming.py`, `evidence/ops_log.py` | Available-now stream metrics aggregation, start/result payloads and child-run metric preference ported from Databricks streaming helpers. Stream start evidence defaults start/capture timestamps, framework/control schema version and zero counters when callers provide only the stream identity/status, and preserves target catalog plus contract-derived idempotency/run hierarchy fields. |
| Available-now stream orchestration | `PORTED` | `runtime/available_now.py`, `runtime/sources.py`, `runtime/streaming.py` | Adapter-owned available-now `foreachBatch` orchestration with injected batch ingestor, checkpoint enforcement and stream lifecycle result payloads. |
| Annotation/access/operations logging | `PORTED` | `evidence/governance_log.py` | Runtime audit rows separate from apply helpers. Governance log renderers preserve the evidence enrichment discipline by defaulting annotation/access timestamps, date partitions, framework/control schema version and operations recorded timestamp when callers do not supply them. |
| State, idempotency and locks | `PORTED` | `state/sql.py`, `state/queries.py`, `state/writer.py`, `runtime/options.py`, `runtime/finalization.py` | State MERGE, idempotent run/stream lookup, contract-derived idempotency options, Databricks lock extension handling, lock read-back and metadata registration. Successful runtime finalization updates `last_success_at_utc`, `last_write_completed_at_utc`, `last_watermark_candidate`, Delta version and hierarchy metadata. Lock acquisition performs the read-back ownership check whenever an injected `query_one` is available, while write-only runners still render/execute the MERGE. Lock release remains best-effort so cleanup failures do not mask the ingestion result. Idempotent run lookup orders by the mature `ctrl_ingestion_runs.run_ts_utc` ledger timestamp; stream lookup orders by `ctrl_ingestion_streams.started_at_utc`. |
| Retry on Delta concurrency | `PORTED` | `execution/retry.py`, `runtime/write_flow.py` | Retry token matching and write-flow retry execution using contract `retry_attempts` and `retry_backoff_seconds`. |
| Execution windows/catchup planning and runtime | `PORTED` | `execution/windows.py`, `runtime/windows.py`, `runtime/bundles.py` | Window generation, Databricks timestamp filter SQL, child idempotency keys, child prepared views, per-window runtime execution and aggregate parent summaries. Child runs record `parent_run_id` and `_contractforge_window_*` runtime parameters in `ctrl_ingestion_runs`/state evidence. Catchup can start from an explicit start value or from previous typed watermark state when a `query_one` reader is supplied. |
| OpenLineage event evidence | `PORTED` | `lineage/`, `runtime/lineage.py` | OpenLineage-compatible payload, insert SQL and optional runtime emission when `extensions.databricks.openlineage_enabled` is true. Runtime events preserve ContractForge facets, parent-run facets, Spark runtime version and source-code/notebook location when supplied by Databricks runtime metadata; parent facets use the supplied Databricks job/notebook location when available. |
| Diagnostics/explain plan evidence | `PORTED` | `diagnostics/`, `runtime/lineage.py` | Runtime captures explain text through injected query runner when `extensions.databricks.explain_mode` is true; adapter persists redacted payload. |
| Cost/throughput signals | `PORTED` | `cost/`, `metrics/`, `runtime/metrics.py` | DBU-estimate, Delta history metric collection through injected query runner, Delta version evidence, row metric normalization and runtime `metrics_source` evidence. Operational cost analysis uses the mature control-table fields from the Databricks adapter baseline, including target/layer/mode/status, contract owner/domain, runtime type, source connector/provider, operations criticality, rows read/written/quarantined and both legacy/current stage-duration breakdowns. Reports can remain query-only or execute through an injected Databricks SQL runner without importing Spark at package import time. |
| Unity Catalog annotations | `PORTED` | `annotations/` | Comments, tags, aliases, PII and lifecycle metadata SQL. |
| Unity Catalog access controls | `PORTED` | `governance/` | Grants, row filters, masks, audit SQL and adapter-owned unmanaged-grant revoke rendering. Column masks do not invent `USING COLUMNS` when the contract omits it, and unmanaged-grant reconciliation is audited as explicit revoke behavior. |
| Governance validation and access drift | `PORTED` | `governance/validation.py`, `governance/drift.py`, `governance/application.py` | Column reference validation, PII consistency checks, current-vs-declared grant drift reports with injected current state, query-runner grant inspection and explicit-confirmation destructive reconciliation. |
| Operations metadata | `PORTED` | `operations/`, `runtime/governance.py` | Ownership/SLA/runbook evidence and ingestion runtime recording through injected Databricks SQL runner. |
| Databricks Asset Bundle generation | `PORTED` | `bundles/`, `rendering/` | Reviewable deployment artifact. |
| Runtime end-to-end `ingest_plan` equivalent | `PORTED` | `runtime/orchestrator.py`, `runtime/models.py`, `runtime/schema.py`, `runtime/write_flow.py`, `runtime/merge_validation.py`, `runtime/governance.py`, `runtime/lineage.py`, `runtime/dry_run.py`, `runtime/success.py`, `runtime/write.py`, `runtime/finalization.py` | Prepared-view orchestration ports planning, idempotency, locks, dry-run schema preview without SQL side effects, optional Delta table setup/sync from prepared schema, schema-change evidence, MERGE source safety preflight, scoped replace-partitions execution, write dispatch, post-write maintenance, annotations/operations runtime side effects, Delta history metric collection, optional explain/OpenLineage evidence, run/error evidence, state updates, default raise-on-failure discipline and granular `ctrl_ingestion_runs` source/runtime payload fields. Public runtime payloads expose ContractForge-compatible summary aliases such as `rows_effective`, `write_engine`, `operation_metrics` and `stage_durations`, while persisted evidence keeps canonical control-table names. Source reading and PySpark preparation remain runtime-owned. |
| Programmatic ingestion hooks | `PORTED` | `contract_extensions.py`, `runtime/hooks.py`, `runtime/options.py`, `runtime/orchestrator.py` | Adapter-owned hooks around prepared input, write execution and finalization. Hooks can transform `PreparedInput` or observe outcomes without adding Spark dependencies to the core. Contract-declared hooks must live under `extensions.databricks.hooks` and be a `DatabricksIngestionHooks` instance or compatible dataclass mapping. |
| Spark utility conveniences | `PORTED` | `runtime/spark.py` | Active Spark resolution, runtime info, serverless-safe cache/unpersist, table existence, schema signature, emergency string encoding fix and Delta schema sync helpers. PySpark imports remain lazy and adapter-owned. |
| SQL/runtime utility conveniences | `PORTED` | `sql/`, `runtime/utils.py` | Identifier quoting, generic SQL literals, JSON literals, UTC/run-id helpers, safe truncation, list normalization and DataFrame column validation are adapter-owned and Spark-import-free. |
| CLI commands | `PORTED` | core `contractforge` CLI; adapter `cli.py`, `cli_governance.py`, `cli_maintenance.py`, `governance/runtime.py` | Generic validation, schema, init and connector diagnostics belong to the core `contractforge` CLI. The Databricks adapter CLI owns Databricks-specific render, dashboard, maintenance, governance SQL review/apply-plan, presets and templates. Adapter render composes split contract bundles and environment contracts through the core bundle loader. Direct governance apply/check is runtime-owned with an injected runner, not CLI-owned. |
| Template catalog/presets | `PORTED` | `presets/`, `templates/` | Ported as Databricks-owned examples/defaults, not core defaults. The template catalog preserves the 18 split-contract examples and restores mature Databricks adapter parameters in canonical form, including REST pagination/incremental metadata, HTTP schemas, JDBC partitioned reads, RDS IAM, canonical `transform.deduplicate`, governed Auto Loader via portable `incremental_files`, and historical/hash-diff exclusions without legacy `ingestion_date`. The preset catalog preserves the 29 Databricks adapter presets across bronze, silver, gold, Delta, quality, governance, runtime and write-engine preview groups, expressed with new canonical source and write-engine names and without carrying legacy physical columns such as `ingestion_date` into hash exclusions by default. |
| Dashboard blueprint | `PORTED` | `dashboards/` | Databricks SQL/Lakeview-oriented control-table dashboard SQL and blueprint artifacts. |

## Technical Row Columns

The Databricks adapter preparation layer added technical columns to target rows:

- `ingestion_date`
- `ingestion_ts_utc`
- `source_system`
- `__run_id`

In ContractForge Core these are not mandatory semantic columns. The durable audit source of truth is the evidence model and Databricks control tables. In particular, `ingestion_date` is treated as a legacy physical partitioning convenience from the Databricks adapter baseline, not as a required ContractForge contract concept.

Databricks presets and runtime behavior must not assume `ingestion_date` exists by default. If a deployment wants physical row metadata columns, the Databricks adapter should expose that as an explicit adapter-owned option rather than silently adding them from the core.

## Source Code Audit Against Databricks adapter baseline

The parity matrix tracks Databricks adapter capability coverage across the implementation surface.

The following Databricks capability domains are covered by implemented core or Databricks adapter domains:

| Databricks area | Current coverage |
| --- | --- |
| `databricks_native/*` | `contractforge_databricks.capabilities`, `lakeflow`, `parity` |
| `writers/*`, `write_engine.py` | `contractforge_databricks.execution`, `write_modes`, `metrics`, `partitioning`; core `execution`, `parity`, `preparation` |
| `state/*` | `contractforge_databricks.state`, `evidence`, `runtime.finalization` |
| `governance/*` | core `contracts.annotations`, `contracts.operations`, `contracts.access`; adapter `annotations`, `operations`, `governance`, `evidence.governance_log` |
| `maintenance/*`, `cost/*` | `contractforge_databricks.maintenance`, `cost`, `dashboards` |
| `templates/*`, `presets/*` | `contractforge_databricks.templates`, `presets` |
| `naming/*`, `watermark/*`, `lineage/*` | core `naming`, `watermark`, `diagnostics`; adapter `rendering.names`, `watermark`, `lineage`, `diagnostics` |
| `contract_schema/*`, `contracts/*`, `contract_bundle/*` | core `contracts`, `normalization`, `semantic`, `planner` |
| `plan/*` write/source/quality normalization | core `contracts`, `connectors`, `normalization`, `planner`; adapter source renderers |

The following Databricks capabilities are tracked after the source audit:

| Databricks capability | Current status | Required direction |
| --- | --- | --- |
| CLI validation and project discovery | `PORTED_IN_CORE` | `contractforge validate`, `validate-bundle` and `validate-project` use core contract models and split bundle composition without executing a platform. |
| Contract JSON/YAML schema generation | `PORTED_IN_CORE` | `contractforge_core.contracts.yaml_schema` exposes the stable root contract schema entry point for IDE/autocomplete and validation tooling. `contract_model_schemas()` remains available for split-section schema fragments, including environment, without mixing environment fields into ingestion contracts. |
| Public core helper API | `PORTED_IN_CORE` | The root `contractforge_core` package exports neutral equivalents for schema generation, bundle loading, target naming, connector metadata diagnostics, naming models, native capability evidence and redaction helpers. Runtime execution classes from the Databricks adapter `IngestionPlan` surface are intentionally not recreated in the core. |
| Public governance API split | `PORTED_IN_CORE_AND_ADAPTER` | Core exports canonical neutral contract models for annotations, operations and access using `*ContractModel` names. Databricks exports governance reference validation, access drift reporting, SQL rendering and apply/check runtime helpers because those depend on Unity Catalog and adapter execution. |
| Public Databricks cost, retention and UC helpers | `PORTED` | The Databricks package exports canonical helpers such as `render_operational_cost_query`, `build_operational_cost_report`, `build_control_retention_plan`, `execute_control_retention_plan` and `uc_capability_issues`. Old abbreviated/legacy aliases are intentionally not part of the new product surface. |
| Public write-engine and shape helpers | `PORTED_IN_CORE_AND_ADAPTER` | Core exports `validate_plan_shape` as static contract validation over semantic contracts. Databricks exports canonical `choose_write_strategy`, `resolve_source_dataframe` and `apply_shape` over the adapter strategy, source resolver and PySpark preparation modules. |
| Legacy public model aliases intentionally not carried forward | `PORTED_WITH_REPLACEMENT` | Because ContractForge Core is a new product and does not need source compatibility, legacy public aliases such as `QualityRules`, `QualityExpression`, `ConnectorSpec`, `SourceSpec`, `TransformConfig`, `ShapeConfig`, `FrameworkConfig`, `IngestionHooks`, `WriteEngineConfig`, `ingest`, `ingest_plan`, `resolve_batch_source`, `apply_ctrl_retention` and `analyze_operational_cost` are replaced by canonical names. The parity document remains the migration map; the runtime API does not keep compatibility aliases. |
| Legacy Spark-first public surface intentionally not recreated | `PORTED_WITH_REPLACEMENT` | `IngestionPlan` is replaced by `SemanticContract` plus split contract models. `FileConnector`, `HttpFileConnector`, `JdbcConnector`, `ObjectStorageConnector` and `RestApiConnector` are replaced by core connector metadata plus Databricks runtime source resolvers. `operational_cost_dataframe` is replaced by query/report generation. `resolve_secrets` remains adapter/environment-owned and must not become a core default. |
| Split contract bundle loading | `PORTED_IN_CORE` | `contractforge_core.contracts.load_contract_bundle` loads ingestion-root bundles from `.yaml`, `.yml` or `.json`, composes annotations/operations/access, preserves environment separately, validates target compatibility and reports metadata version warnings. |
| CLI contract initialization | `PORTED_IN_CORE` | `contractforge init` writes split ingestion, annotations, operations, access and environment contracts. The adapter value is environment metadata, not execution logic. |
| CLI connector registry/doctor | `PORTED_IN_CORE` | `contractforge connectors list/show/doctor` exposes core source taxonomy and portability diagnostics. Databricks-specific aliases remain adapter documentation/rendering concerns. |
| CLI governance preview/check/apply | `PORTED` | `contractforge-databricks governance-preview` and `governance-apply-plan` render Unity Catalog SQL and operations evidence from core split contracts. `governance/runtime.py` provides direct check/apply with an injected Databricks SQL runner; the CLI remains a review/planning surface instead of becoming the executor. |
| Runtime source resolver execution | `PORTED` | `runtime/sources.py`, `runtime/http_file.py` and `runtime/rest_api.py` resolve Spark-native catalog, file/object-storage, HTTP file, bounded REST API, JDBC, Auto Loader/incremental files, bounded Kafka/Event Hubs and Delta Sharing sources with injected Spark. File/object-storage sources include XML-format parity through core `source.type: xml` or `source.format: xml`, with parser options passed through `source.options`. JDBC sources consume core-normalized options, including incremental watermark predicate pushdown over `dbtable` or `query`. Prepared source metadata now uses the core metadata helper so redaction, source capabilities and `source_complete` semantics are consistent between review artifacts and runtime evidence. `prepare_contract_source_view` applies core preparation fields before registering the temp view. Native passthrough remains an explicit review/runtime-extension boundary. |
| Runtime source resolver registry | `PORTED` | `runtime/source_registry.py`, `runtime/sources.py`, package public API | Provides the custom resolver extension point as Databricks runtime-owned behavior. Custom resolvers receive injected `spark` and source mappings, so DataFrame/Spark concepts do not leak into the core. `register_source_resolver`, `get_source_resolver`, `list_source_resolvers`, `unregister_source_resolver` and `resolve_source_dataframe` are exposed through the Databricks adapter public API. |
| Split bundle runtime ingestion and governance | `PORTED` | `runtime/bundles.py` loads split bundles through the core bundle loader, consumes canonical `extensions.databricks`, prepares source views with injected Spark and runs the Databricks adapter runtime. It also provides adapter-owned governance/annotations/access bundle executors with injected SQL runner. Bundle execution stays adapter-owned; core remains contract composition only. |
| Source connector metadata catalog | `PORTED_IN_CORE` | `contractforge_core.connectors.catalog`, `metadata.py`, `cli_connectors.py` provide connector metadata shape for families, requirements, alternatives, auth modes, supported formats, providers, limits, recommendations and runtime notes. The portable file vocabulary preserves XML format coverage while keeping XML parser/runtime caveats as adapter capability warnings. Databricks consumes the neutral metadata through source evidence and keeps platform execution in the adapter. |
| Source connector executable validation | `PORTED_IN_CORE` | `contractforge_core.contracts.source_validation`, `cli_contracts.py` port platform-neutral validation for executable source fields, JDBC table/query and partitioning rules, HTTP file method/format constraints, canonical `auth.type` handling, REST pagination/response constraints including raw-response column safety, bounded stream requirements, object-storage provider/format checks and positive limit values. Core source validation requires canonical schema declarations in `source.read.schema`; top-level `source.schema` is rejected and `source.options.schema` is treated as an adapter option that must not replace the canonical field. JDBC authentication uses canonical `auth.type: basic` or `auth.type: rds_iam`; old `auth.method`, `password`, `user_password`, `auth.user`, `auth.token` and HTTP API-key value aliases are migration/documentation concerns, not runtime aliases. The CLI applies these checks to real contract files while the base semantic model still allows abstract connector intent for planning. Databricks-specific Auto Loader validation is represented by portable `incremental_files` in core and adapter-owned `cloudFiles` rendering/runtime. |
| Connector redaction discipline | `PORTED_IN_CORE_AND_ADAPTER` | `contractforge_core.security` ports mature redaction for sensitive keys, secret placeholders, Basic/Bearer headers, URL userinfo, sensitive query parameters, assignment-style secrets and private key blocks. Secret resolution remains adapter/runtime-owned: the Databricks runtime resolves `{{ secret:scope/key }}` from `CONTRACTFORGE_SECRET_<SCOPE>_<KEY>` environment variables or Databricks `dbutils.secrets` before constructing JDBC/HTTP/REST runtime options. |
| Public failure-result exceptions | `PORTED_IN_CORE` | `contractforge_core.errors` ports `ContractForgeExecutionError` and `raise_for_failure_result` so callers can enforce failed-result handling without importing a platform adapter. Databricks runtime results can be checked through the same core API. |
| Portable execution/idempotency contract fields | `PORTED_IN_CORE` | `contracts/execution.py`, `contracts/root.py`, `normalization/intents.py` port execution windows, catchup, filters, watermarks, idempotency, retry and run grouping as core contract semantics. Adapters decide how those fields become native runtime behavior. |
| Preset expansion metadata | `PORTED_IN_CORE` | `contracts/root.py`, `normalization/intents.py` accept `applied_presets` as platform-neutral contract metadata. The Databricks runtime includes it in the public run payload and `contract_metadata`, matching the Databricks adapter baseline result surface without making presets an execution concern. |
| Programmatic preset registration | `PORTED` | `presets/core.py`, `presets/__init__.py`, package public API | Databricks adapter exposes `register_preset` for organization-specific preset defaults, preserving the Databricks adapter extension point without making presets a core execution feature. Built-in presets remain adapter-owned because they include Databricks/Delta/runtime defaults. |
| Programmatic write-mode registration | `PORTED` | `write_modes/registry.py`, `runtime/write.py`, package public API | Provides the runtime extension point as adapter-owned behavior. Custom modes must use the explicit `custom:<name>` contract namespace; the core treats them as non-portable semantics and only plans them as supported when the adapter declares a registered handler. |
| Observability metadata | `PORTED_WITH_CANONICAL_ENVIRONMENT` | `contracts.source_*`, `connectors.metadata`, `runtime/finalization.py`, `environment.py` preserve canonical `source.system` as evidence metadata. Runtime artifact names such as `notebook_name` now come from Databricks runtime metadata, not the core contract. Evidence schema selection moved to the environment contract (`environment.evidence.catalog/schema`) or explicit Databricks runtime options; legacy root aliases `source_system`, `ctrl_schema` and `notebook_name` are intentionally rejected in the new product. The core does not recreate automatic `source_system` physical columns. |
| REST API driver client and pagination execution | `PORTED` | `runtime/rest_api.py`, `runtime/http_retry.py`, `runtime/json_materialization.py` port bounded JSON REST execution with GET/POST, bearer/API-key/basic/OAuth client-credentials auth, page/offset/cursor/link-header pagination, retry only for retryable HTTP/network failures, byte and record limits, raw/records response modes, incremental watermark query/header/body injection and Spark materialization. Complex JSON records use the Spark JSON-lines materialization path when available. Bundle rendering still defaults to review artifacts so proprietary SaaS APIs can be routed to native connectors deliberately. |
| Programmatic ingestion hooks | `PORTED` | `DatabricksIngestionHooks` provides adapter-owned callbacks around prepared source, write and finalization without introducing Spark dependencies into core. Hooks are adapter-owned extension data under `extensions.databricks.hooks` and are never interpreted by the core. |
| Custom PySpark quality rule registry | `PORTED` | `quality/registry.py` and `quality/evaluation.py` port custom evaluator registration, lookup/list/unregister lifecycle helpers, result normalization and runtime integration with `evaluate_quality()`. Custom evaluators may return `condition` for `severity=quarantine`; missing conditions are rejected so quarantine behavior is never silently downgraded. |
| Custom Databricks write-mode registry | `PORTED` | `write_modes/registry.py` ports custom prepared-write handlers behind explicit `custom:<name>` modes. The registry is exposed through the Databricks public API, while the core keeps unknown custom modes as review boundaries unless an adapter declares support. |
| Built-in PySpark quality evaluation | `PORTED` | `quality/evaluation.py` ports runtime evaluation for required columns, not-null, accepted values, max-null ratio, unique key, row-count minimum and expression checks with quarantine split support. PySpark remains dynamically resolved and adapter-owned. |
| Executable shape/transform PySpark parity | `PORTED` | Transform execution is ported in `preparation/pyspark.py` for select columns, column mapping, filter expression, cast, standardize, derive and deduplicate. Shape execution is ported in `preparation/shape.py` and `preparation/zip_arrays.py` for parse_json with inline schema or core-resolved `schema_ref`, zip_arrays with contract-declared struct field aliases, array modes, nested-array sequencing, projection, flatten include/exclude/max_depth, bronze cardinality policy, cartesian explode protection, schema-aware type validation for supported operations and intermediate-column cleanup. Adapter-owned emergency encoding repair runs last through `preparation/encoding.py` when `extensions.databricks.fix_encoding` is enabled, preserving the Databricks adapter baseline preparation order without making encoding a core semantic. |
| Spark utility conveniences | `PORTED` | `runtime/spark.py` ports `safe_cache`, `safe_unpersist`, `table_exists`, `schema_signature`, `fix_encoding`, runtime info and DataFrame-level Delta schema sync as Databricks adapter runtime helpers. |
| SQL/runtime utility conveniences | `PORTED` | `sql/` and `runtime/utils.py` port SQL literals, JSON literal serialization, UTC/run-id helpers, truncation, list parsing and column validation without requiring PySpark imports. |
| Unity Catalog capability issues | `PORTED` | `capabilities/uc.py` ports conservative issue reporting for comments, tags, grants, row filters and masks when the target/capability evidence cannot safely support the declared governance feature. |

## Non-Negotiable Rule

If a Databricks capability existed in the Databricks adapter baseline and is not ported yet, it must appear in this matrix as `NOT_PORTED_YET` or `PORT_IN_PROGRESS`.

No capability should be silently dropped just because the core is now platform-neutral.

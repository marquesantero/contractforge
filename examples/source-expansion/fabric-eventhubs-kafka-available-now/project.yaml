name: fabric_eventhubs_kafka_available_now_source_smoke
description: Fabric source-expansion smoke for Azure Event Hubs Kafka-compatible available-now reads.
default_environment: fabric

environments:
  fabric: environments/fabric.environment.yaml

execution_order:
  - name: eventhubs_kafka_available_now_orders
    layer: bronze
    expected_result: succeeded
    depends_on: []
    contracts:
      fabric: contracts/01_eventhubs_kafka_available_now_orders.ingestion.yaml
  - name: eventhubs_kafka_available_now_evidence_probe
    layer: bronze
    expected_result: succeeded
    depends_on: [eventhubs_kafka_available_now_orders]
    contracts:
      fabric: contracts/02_eventhubs_kafka_available_now_evidence_probe.ingestion.yaml

source_expansion:
  source_family: eventhubs_kafka_available_now
  authentication: sasl_plain_key_vault_placeholder
  trigger: available_now
  checkpoint_location: Files/checkpoints/contractforge/fabric-eventhubs-kafka-available-now/orders-20260613
  expected_success_steps: 2
  expected_probe_checks: 6
  stable_final_scope: stable_f11_eventhubs_kafka_available_now_evidence


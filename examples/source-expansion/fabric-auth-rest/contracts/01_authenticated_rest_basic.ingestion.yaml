source:
  type: rest_api
  request:
    url: https://postman-echo.com/basic-auth
    method: GET
    headers:
      Accept: application/json
      User-Agent: ContractForge Fabric authenticated REST source smoke
  auth:
    type: basic
    username: postman
    password: "{{ secret:fabric/postman-basic-password }}"
  limits:
    timeout_seconds: 60
    retry_attempts: 3
    retry_backoff_seconds: 2
    max_page_bytes: 1048576
    max_total_bytes: 1048576
    max_records: 10
target:
  catalog: workspace
  schema: cf_fabric_source_expansion
  table: auth_rest_basic
layer: bronze
mode: overwrite
schema_policy: permissive
quality_rules:
  required_columns: [authenticated]
  not_null: [authenticated]
  min_rows: 1
  expressions:
    - name: authenticated_true
      expression: authenticated = true
      severity: abort

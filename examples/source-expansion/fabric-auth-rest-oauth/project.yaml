name: fabric_authenticated_rest_oauth_source_smoke
description: Fabric source-expansion smoke for OAuth client-credentials bounded REST reads.
default_environment: fabric

environments:
  fabric: environments/fabric.environment.yaml

execution_order:
  - name: authenticated_rest_oauth
    layer: bronze
    expected_result: succeeded
    depends_on: []
    contracts:
      fabric: contracts/01_authenticated_rest_oauth.ingestion.yaml
  - name: authenticated_rest_oauth_evidence_probe
    layer: bronze
    expected_result: succeeded
    depends_on: [authenticated_rest_oauth]
    contracts:
      fabric: contracts/02_authenticated_rest_oauth_evidence_probe.ingestion.yaml

source_expansion:
  source_family: rest_api
  authentication: oauth_client_credentials_key_vault_placeholder
  expected_success_steps: 2
  expected_probe_checks: 6
  stable_final_scope: stable_f11_authenticated_rest_oauth_evidence

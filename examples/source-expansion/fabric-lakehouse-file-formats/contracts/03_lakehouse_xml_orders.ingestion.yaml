source:
  type: xml
  path: Files/source-expansion/lakehouse-file-formats/xml_orders
  options:
    rowTag: order
target:
  catalog: workspace
  schema: cf_fabric_source_expansion
  table: lakehouse_xml_orders
layer: bronze
mode: overwrite
schema_policy: permissive
quality_rules:
  required_columns: [order_id, amount]
  not_null: [order_id, amount]
  min_rows: 2
  expressions:
    - name: positive_amount
      expression: amount > 0
      severity: abort


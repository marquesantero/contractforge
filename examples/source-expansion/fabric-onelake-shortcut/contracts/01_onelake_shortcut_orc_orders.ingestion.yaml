source:
  type: orc
  path: Files/source-expansion/onelake-shortcuts/source_expansion_shortcut/lakehouse-file-formats/orc_orders
target:
  catalog: workspace
  schema: cf_fabric_source_expansion
  table: onelake_shortcut_orc_orders
layer: bronze
mode: overwrite
schema_policy: permissive
quality_rules:
  required_columns: [order_id, amount]
  not_null: [order_id, amount]
  min_rows: 2
  expressions:
    - name: positive_amount
      expression: amount > 0
      severity: abort

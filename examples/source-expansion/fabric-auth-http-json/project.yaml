name: fabric_authenticated_http_json_source_smoke
description: Fabric source-expansion smoke for authenticated bounded HTTP JSON reads.
default_environment: fabric

environments:
  fabric: environments/fabric.environment.yaml

execution_order:
  - name: authenticated_http_json_basic
    layer: bronze
    expected_result: succeeded
    depends_on: []
    contracts:
      fabric: contracts/01_authenticated_http_json_basic.ingestion.yaml
  - name: authenticated_http_json_evidence_probe
    layer: bronze
    expected_result: succeeded
    depends_on: [authenticated_http_json_basic]
    contracts:
      fabric: contracts/02_authenticated_http_json_evidence_probe.ingestion.yaml

source_expansion:
  source_family: http_json
  authentication: basic_key_vault_placeholder
  expected_success_steps: 2
  expected_probe_checks: 6
  stable_final_scope: stable_f11_authenticated_http_json_evidence

annotations:
  policy: warn
  table:
    description: "Silver NASA EONET with one row per geographic event observation."
    aliases: [nasa_eonet_event_observations]
    tags:
      domain: natural-events
      layer: silver
      shape: parse_json_arrays_columns_expressions
      contains_pii: "false"
  columns:
    event_id:
      description: "EONET event identifier."
    category_title:
      description: "Main event category."
    observed_at:
      description: "Geographic observation timestamp."
    longitude:
      description: "Longitude extracted from the coordinates array."
    latitude:
      description: "Latitude extracted from the coordinates array."

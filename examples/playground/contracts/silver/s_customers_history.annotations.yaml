target:
  catalog: main
  schema: crm_curated
  table: s_customers_history

table:
  description: Customer SCD2 history.
  tags:
    domain: crm
    history: scd2

columns:
  customer_id:
    description: Customer key.
  email:
    description: Customer email.
    pii:
      enabled: true
      type: email
      sensitivity: restricted

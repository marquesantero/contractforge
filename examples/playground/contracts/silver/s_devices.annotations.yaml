target:
  catalog: main
  schema: iot_curated
  table: s_devices

table:
  description: Current device state with soft delete.
  tags:
    domain: iot
    pattern: snapshot_soft_delete

columns:
  device_id:
    description: Unique device identifier.

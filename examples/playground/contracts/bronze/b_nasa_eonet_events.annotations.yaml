annotations:
  policy: warn
  table:
    description: "Raw Bronze NASA EONET v3 response, one row per REST page."
    aliases: [nasa_eonet_events_raw]
    tags:
      domain: natural-events
      layer: bronze
      source: nasa-eonet
      source_connector: rest_api
      response_mode: raw
      contains_pii: "false"
  columns:
    raw_response:
      description: "Raw JSON payload returned by the REST endpoint."
    response_page_number:
      description: "Sequential page/response number downloaded by the connector."

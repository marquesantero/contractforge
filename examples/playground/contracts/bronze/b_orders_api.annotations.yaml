target:
  catalog: main
  schema: bronze
  table: b_orders_api

table:
  description: Orders received from the external API in Bronze format.
  tags:
    domain: sales
    source: rest_api

columns:
  id:
    description: API order identifier.
  updated_at:
    description: Update timestamp used as the watermark.
